import styled from 'styled-components';
import { PageSection, ImageContainer } from '../sharedStyling';

export const Section = styled.section`
  ${PageSection};
  background: linear-gradient(#DAD7D0, #F5F5F5);
`;

export const Container = styled.div`
  ${ImageContainer}  
`;